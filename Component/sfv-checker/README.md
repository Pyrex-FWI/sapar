

```

$sfvChecker = SfvChecker::create(
                                new FileSystemReader(), 
                                '/path/to/sfv/file.sfv
                            );
$sfvChecker->isCheckable();
```
